# URL base del backend (API REST) que expone /predict/single, /predict/range y /retrain.
# Si se deja vacía, la app funciona en modo mock con datos simulados.
VITE_API_URL=

# Forzar modo mock aunque haya una VITE_API_URL configurada (útil para demos).
VITE_USE_MOCK=false
