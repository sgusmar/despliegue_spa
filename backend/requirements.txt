# Generado desde un venv limpio con Python 3.13, tras pasar la suite de tests.
#
# scikit-learn va fijado a 1.8.0 a propósito: es la versión con la que se
# serializaron los .joblib de models/. Al cargarlos con otra versión, sklearn
# avisa de que el unpickle "might lead to breaking code or invalid results",
# y con un modelo en producción eso no se deja al azar.

Flask==3.1.3
gunicorn==26.2.0
holidays==0.104
joblib==1.6.0
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.8.0
scipy==1.18.1

# Transitivas, fijadas para que el build de Render sea reproducible.
blinker==1.9.0
click==8.5.0
cloudpickle==3.1.2
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
python-dateutil==2.9.0.post0
six==1.17.0
threadpoolctl==3.6.0
tzdata==2026.3
Werkzeug==3.1.8
